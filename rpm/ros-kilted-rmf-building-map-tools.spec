%bcond_without tests
%bcond_without weak_deps

%global __os_install_post %(echo '%{__os_install_post}' | sed -e 's!/usr/lib[^[:space:]]*/brp-python-bytecompile[[:space:]].*$!!g')
%global __provides_exclude_from ^/opt/ros/kilted/.*$
%global __requires_exclude_from ^/opt/ros/kilted/.*$

Name:           ros-kilted-rmf-building-map-tools
Version:        1.12.0
Release:        1%{?dist}%{?release_suffix}
Summary:        ROS rmf_building_map_tools package

License:        Apache License 2.0
Source0:        %{name}-%{version}.tar.gz

Requires:       python%{python3_pkgversion}-pyproj
Requires:       python%{python3_pkgversion}-requests
Requires:       python%{python3_pkgversion}-yaml
Requires:       python3-fiona
Requires:       python3-rtree
Requires:       python3-shapely
Requires:       ros-kilted-ament-index-python
Requires:       ros-kilted-gz-fuel-tools-vendor
Requires:       ros-kilted-rclpy
Requires:       ros-kilted-rmf-building-map-msgs
Requires:       ros-kilted-rmf-site-map-msgs
Requires:       ros-kilted-std-msgs
Requires:       sqlite
Requires:       yaml-cpp-devel
Requires:       ros-kilted-ros-workspace
BuildRequires:  python%{python3_pkgversion}-devel
BuildRequires:  ros-kilted-rmf-building-map-msgs
BuildRequires:  ros-kilted-ros-workspace
Provides:       %{name}-devel = %{version}-%{release}
Provides:       %{name}-doc = %{version}-%{release}
Provides:       %{name}-runtime = %{version}-%{release}

%if 0%{?with_tests}
BuildRequires:  python%{python3_pkgversion}-pytest
%endif

%description
RMF Building map tools

%prep
%autosetup -p1

%build
# In case we're installing to a non-standard location, look for a setup.sh
# in the install tree and source it.  It will set things like
# CMAKE_PREFIX_PATH, PKG_CONFIG_PATH, and PYTHONPATH.
if [ -f "/opt/ros/kilted/setup.sh" ]; then . "/opt/ros/kilted/setup.sh"; fi
%py3_build

%install
# In case we're installing to a non-standard location, look for a setup.sh
# in the install tree and source it.  It will set things like
# CMAKE_PREFIX_PATH, PKG_CONFIG_PATH, and PYTHONPATH.
if [ -f "/opt/ros/kilted/setup.sh" ]; then . "/opt/ros/kilted/setup.sh"; fi
%py3_install -- --prefix "/opt/ros/kilted"

%if 0%{?with_tests}
%check
# Look for a directory with a name indicating that it contains tests
TEST_TARGET=$(ls -d * | grep -m1 "\(test\|tests\)" ||:)
if [ -n "$TEST_TARGET" ] && %__python3 -m pytest --version; then
# In case we're installing to a non-standard location, look for a setup.sh
# in the install tree and source it.  It will set things like
# CMAKE_PREFIX_PATH, PKG_CONFIG_PATH, and PYTHONPATH.
if [ -f "/opt/ros/kilted/setup.sh" ]; then . "/opt/ros/kilted/setup.sh"; fi
%__python3 -m pytest $TEST_TARGET || echo "RPM TESTS FAILED"
else echo "RPM TESTS SKIPPED"; fi
%endif

%files
/opt/ros/kilted

%changelog
* Tue May 20 2025 Morgan Quigley <mquigley@openrobotics.org> - 1.12.0-1
- Autogenerated by Bloom

* Tue Apr 22 2025 Morgan Quigley <mquigley@openrobotics.org> - 1.11.0-2
- Autogenerated by Bloom

* Wed Nov 27 2024 Morgan Quigley <mquigley@openrobotics.org> - 1.11.0-1
- Autogenerated by Bloom

* Sat Jun 15 2024 Morgan Quigley <mquigley@openrobotics.org> - 1.10.0-1
- Autogenerated by Bloom

* Mon Jun 03 2024 Morgan Quigley <mquigley@openrobotics.org> - 1.9.0-1
- Autogenerated by Bloom

